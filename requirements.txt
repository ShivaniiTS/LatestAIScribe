# Runtime dependencies — used in production Docker image
# GPU extras (faster-whisper, torch, whisperx) are NOT included here.
# Install those separately on GPU hosts: pip install -e ".[gpu]"

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Database
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13.0

# Orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM / HTTP
httpx>=0.27.0

# Configuration
pyyaml>=6.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0

# MT workflow
paramiko>=3.4.0
python-docx>=1.1.0

# Utils
python-dotenv>=1.0.1
aiofiles>=23.2.1
